@using SnipEx.Web.ViewModels.Post
@model PostDetailsViewModel

@{
    ViewData["Title"] = Model.Title + " - SnipEx";
}

<div class="container">
    <div class="post-header">
        <h1 class="post-title">@Model.Title</h1>
        <div class="post-meta">
            <div class="post-author">Posted by @Model.UserName</div>
            <div class="post-date">@Model.CreatedAt.ToString("dd/MM/yyyy")</div>
        </div>
        <div class="post-tags">
            @foreach (var tag in Model.Tags)
            {
                <span class="post-tag">@tag</span>
            }
        </div>
    </div>

    <div class="code-snippet">
        <button class="copy-button" onclick="copyToClipboard()">Copy</button>
        <pre><code class="language-@Model.LanguageName">@Model.Content</code></pre>
    </div>

    <div class="comments-section">
        <div class="comments-header">
            <h2 class="comments-title">Comments (@Model.Comments.Count)</h2>
        </div>

        <form asp-action="AddComment" asp-controller="Post" method="post">
            <input type="hidden" name="PostId" value="@Model.Id" />
            <textarea name="CommentContent" class="comment-input" placeholder="Share your thoughts..." required></textarea>
            <button type="submit" class="submit-comment">Post Comment</button>
        </form>

        @foreach (var comment in Model.Comments)
        {
            <div class="comment">
                <div class="comment-header">
                    <div class="comment-author">@comment.UserName</div>
                    <div class="comment-date">@comment.CreatedAt.ToString("dd/MM/yyyy HH:mm")</div>
                </div>
                <div class="comment-body">
                    @comment.Content
                </div>
            </div>
        }
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/post/details.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/themes/prism-tomorrow.min.css" />
}

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/prism.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/components/prism-csharp.min.js"></script>
    <script src="~/js/post/details.js"></script>
}